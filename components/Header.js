import React from "react";
import { Header } from "react-native-elements";

export default () => (
  <Header
    centerComponent={{
      text: "iTunes Sampler",
      style: { color: "#fff", fontSize: 20 }
    }}
  />
);
