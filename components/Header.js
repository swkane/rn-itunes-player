import React from "react";
import { Header } from "react-native-elements";

export default () => (
  <Header
    centerComponent={{
      text: "iTunes Player",
      style: { color: "#fff", fontSize: 20 }
    }}
  />
);
